Add 'defer' or 'async' to JS files for make page loading faster. 

If you want to know which which script tags your site is using, add the code in 'get-all-script-tags.php' to the functions.php. Make sure you do this on a staging site because the results might be visible on the front-end. 

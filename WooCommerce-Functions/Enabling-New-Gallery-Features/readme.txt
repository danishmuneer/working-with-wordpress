You need both the CSS and PHP files

CSS code goes in the current WordPress theme's style.css

PHP code goes in the current theme's functions.php file

The gallery was added as a core WooCoommerce feature after the 3.x version. In the PHP file, you can comment out any specific feature (zoom, slider, lightbox) you don't want. The CSS usually needs slight adjustment depending upon the site's requirements. 
